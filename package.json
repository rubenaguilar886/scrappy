{
  "name": "scrappy",
  "version": "0.1.0",
  "description": "Scraper de directorios de negocios con interfaz web",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "setup": "npm install && npx playwright install chromium"
  },
  "dependencies": {
    "express": "^4.21.0",
    "playwright": "^1.48.0"
  }
}
